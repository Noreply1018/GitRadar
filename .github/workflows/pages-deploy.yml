name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build GitRadar Pages
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Set up Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build web console
        env:
          VITE_GITRADAR_BASE: /${{ github.event.repository.name }}/
          VITE_GITRADAR_REPO_OWNER: ${{ github.repository_owner }}
          VITE_GITRADAR_REPO_NAME: ${{ github.event.repository.name }}
          VITE_GITRADAR_REPO_BRANCH: ${{ github.event.repository.default_branch }}
        run: npm run build:web

      - name: Configure Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@5b2b6b9638ed5dded3b4297f4e5c61a1fbd8a1af
        with:
          path: web/dist

      - name: Deploy Pages
        uses: actions/deploy-pages@6bd01c6fb5f42d69e11923378bcb3d93910fba5c
